import ls from './ls';
import cd from './cd';
import clear from './clear';
import touch from './touch';
import mkdir from './mkdir';
import rm from './rm';

export default {
  ls,
  cd,
  clear,
  touch,
  mkdir,
  rm,
};
